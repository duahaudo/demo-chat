import { Box, Heading, Text } from '@chakra-ui/react';

/**
 * Placeholder shell. Phase 4 builds the real presentation layer.
 */
export function App() {
  return (
    <Box padding="6" color="fg">
      <Heading size="lg">Chat</Heading>
      <Text color="fg.muted">Scaffold. No conversation yet.</Text>
    </Box>
  );
}
